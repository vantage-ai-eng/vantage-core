# vantage-core 0.1.17

**Tag:** `vantage-core-v0.1.17`

**Vantage blue on Control Center + demo** — interactive DEMO chrome and `center.html` use brand blue (`#1375d9`), not teal. Same DEMO labeling as 0.1.16.

## Try this

```bash
python3 -m pip install --no-cache-dir -U vantage-core
vantage-core --version   # 0.1.17

vantage-core demo --interactive
# badge: DEMO · 0.1.17 · Vantage blue banner / accents
```

## What changed

- Demo console + Control Center HTML accents → `--v-brand` blue (`#1375d9` / `#e8f4fd`).
- Pass / block / review colors unchanged (status, not brand).

## What this is not

No hosted dashboard. No change to the CI gate or Coverage semantics.
